# The init.sh file should be exectuted before the launch.sh file

# You should be connected as root to setup the project

# Steps :

`bash VM/scripts/backend/init.sh`
`bash VM/scripts/backend/launch.sh`